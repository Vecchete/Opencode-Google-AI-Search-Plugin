{
  "name": "opencode-google-ai-search-plugin",
  "version": "0.2.0",
  "description": "An opencode plugin that exposes a Google AI Mode search tool.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    "./server": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "keywords": [
    "opencode",
    "plugin",
    "google",
    "ai",
    "search"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "playwright": "latest",
    "turndown": "latest"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "latest"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "latest",
    "@types/turndown": "^5.0.6",
    "typescript": "latest"
  }
}
